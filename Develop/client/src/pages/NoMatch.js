import React from "react";

// When you just have UI, you can use a function to create a page like this.

// This is a simple way to create a static page! 

const NoMatch = () => {
    return (
        <div>
            <h1>Oops, Nothing Here!</h1>
        </div>
    );
}

export default NoMatch;
